# Core binary (libsingbox.so)

Place arm64-v8a `libsingbox.so` in:

```
app/src/main/jniLibs/arm64-v8a/libsingbox.so
```

Without it the app runs in **SAFE MODE** (UI works, no real tunnel).

See `docs/RELEASE_BINARIES.md` for Git LFS / Release delivery to CI.
