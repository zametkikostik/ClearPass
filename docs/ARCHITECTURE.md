# Architecture

VpnService TUN + sing-box SOCKS (localhost auth) + hev TUN→SOCKS (JNI preferred / CLI fallback).

Curated sources: igareck white/black lists + mirrors; backups Stintik, FreeProxyList.

Not a full-internet crawler.
